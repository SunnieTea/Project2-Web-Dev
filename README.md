# Questions
* What do you think the onclick does? <br>
I think that it tells the compiler to do something when a button is clicked.
<br><br>
* Why is it important for each radio button to have a unique ID? <br>
So that you can label them all differently.
<br><br>
* How can we ensure that only one character can be selected at a time? <br>
By giving them all the same name attribute.
<br><br>
* What could happen if the labels aren't correctly associated with the radio buttons? <br>
Then clicking on the label won't actually check the radio button. <br><br>
* Why do you think checkboxes were used instead of a dropdown menu for selecting supplies? What advantages or disadvantages does this approach offer? <br>
Checkboxes were used so that people can select multiple provisons at a time, instead of just one. <br><br>
* The common stylesheet is keeping the button blue. Override this behaviour to set color as white and background-color as red. How would you do this without touching the main style.css file? <br>
By using inline styles on the button.